
class cotactModal{

  String ? title;
  String ? image;
  String ?subtitle;
  bool ?isgroup;
  String ?recenttime;
  bool isselected=false;

  cotactModal({
    this.title,
    this.image,
    this.subtitle,
    this.isgroup,
    this.recenttime,
    this.isselected=false
  });

}